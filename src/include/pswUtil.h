/* pswUtil.h */

// Checks if a string contains only a pure number. 1 - true, 0 - false.
int u_checkIfPureNum(const char *s);

// Gets a string containing a pure number and returns it as an integer.
int u_parseInt(const char *s);

